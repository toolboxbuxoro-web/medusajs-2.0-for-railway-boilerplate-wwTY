# 🚀 Быстрый старт: Модуль отзывов

## ⚡ Что нужно сделать СЕЙЧАС:

### 1️⃣ Запустить миграции (ОБЯЗАТЕЛЬНО!)
```bash
cd backend
npm run build
medusa migrations run
```

**Что это делает:** Создает таблицу `review` в базе данных со всеми полями и индексами.

### 2️⃣ Перезапустить сервер
```bash
cd backend
npm run dev
```

**Проверьте логи:** Не должно быть ошибок о модуле "reviews".

### 3️⃣ Проверить, что всё работает
```bash
# Откройте в браузере страницу любого товара
http://localhost:8000/products/[product-id]

# Прокрутите до секции отзывов
# Если вы авторизованы - должна быть форма для отзыва
```

## ✅ Как проверить, что модуль работает:

### Тест 1: API endpoint
```bash
curl http://localhost:9000/store/products/YOUR_PRODUCT_ID/reviews
```
Должен вернуть JSON (даже если отзывов нет).

### Тест 2: База данных
```bash
psql $DATABASE_URL
\d review
```
Должна быть таблица `review` со всеми полями.

### Тест 3: Логи сервера
При запуске сервера не должно быть ошибок типа:
- "Cannot find module 'reviews'"
- "Module reviews failed to load"

## 🐛 Если что-то не работает:

### Проблема: "Module not found"
**Решение:**
```bash
cd backend
npm run build
# Перезапустите сервер
```

### Проблема: "Table review does not exist"
**Решение:**
```bash
cd backend
medusa migrations run
```

### Проблема: "Cannot resolve reviews"
**Решение:**
Проверьте `medusa-config.js` - должен быть:
```javascript
{
  key: "reviews",
  resolve: "./src/modules/reviews",
}
```

## 📚 Подробная инструкция

См. `TESTING_REVIEWS_MODULE.md` для полного тестирования всех функций.

## 🎯 Что дальше:

1. ✅ Миграции запущены
2. ✅ Сервер работает
3. ✅ API отвечает
4. ⏭️ Создать тестовый отзыв через форму на сайте
5. ⏭️ Проверить модерацию в админ-панели

---

**Вопросы?** Проверьте логи сервера - там будет информация об ошибках.
