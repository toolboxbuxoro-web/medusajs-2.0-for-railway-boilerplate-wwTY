# 🔍 Диагностика: Почему команды не выполняются

## Проблема с psql

Команда `psql $DATABASE_URL` может не работать по нескольким причинам:

1. **Переменная DATABASE_URL не установлена** в текущей сессии shell
2. **psql не установлен** или не в PATH
3. **Нет доступа к базе данных**

## ✅ Альтернативные способы проверки

### Способ 1: Проверить через .env файл
```bash
cd backend
cat .env | grep DATABASE_URL
```

### Способ 2: Проверить через Node.js скрипт
Создайте временный скрипт для проверки БД:

```bash
cd backend
cat > check-db.js << 'EOF'
const { Client } = require('pg');
require('dotenv').config();

const client = new Client({
  connectionString: process.env.DATABASE_URL
});

async function check() {
  try {
    await client.connect();
    const result = await client.query(`
      SELECT column_name, data_type 
      FROM information_schema.columns 
      WHERE table_name = 'review'
      ORDER BY ordinal_position;
    `);
    console.log('Таблица review существует!');
    console.log('Колонки:', result.rows);
    
    // Проверить индексы
    const indexes = await client.query(`
      SELECT indexname, indexdef 
      FROM pg_indexes 
      WHERE tablename = 'review';
    `);
    console.log('\nИндексы:');
    indexes.rows.forEach(idx => console.log(`- ${idx.indexname}`));
    
    await client.end();
  } catch (error) {
    console.error('Ошибка:', error.message);
  }
}

check();
EOF

node check-db.js
```

### Способ 3: Проверить через API (после запуска сервера)
```bash
# После запуска сервера
curl http://localhost:9000/store/products/YOUR_PRODUCT_ID/reviews
```

### Способ 4: Проверить логи миграций
Миграция уже выполнена успешно! В логах было:
```
MODULE: reviews
  ● Migrating Migration20260123000000
  ✔ Migrated Migration20260123000000
Completed successfully
```

Это означает, что таблица **уже создана**!

## ✅ Что уже работает

1. ✅ **Компиляция**: Успешна
2. ✅ **Миграции**: Выполнены успешно
3. ✅ **Таблица**: Создана (судя по логам миграций)

## 🚀 Что делать дальше

### 1. Запустить сервер
```bash
cd backend
npm run dev
```

**Проверить в логах**:
- Нет ошибок о модуле "reviews"
- Сервер запустился на порту 9000

### 2. Протестировать API
```bash
# В другом терминале
curl http://localhost:9000/store/products/YOUR_PRODUCT_ID/reviews
```

**Ожидаемый результат**: JSON с пустым массивом отзывов

### 3. Если нужно проверить БД напрямую

**Вариант A**: Использовать GUI инструмент (pgAdmin, DBeaver, TablePlus)

**Вариант B**: Подключиться через psql вручную:
```bash
# Узнать DATABASE_URL из .env
cd backend
grep DATABASE_URL .env

# Подключиться вручную (замените на ваш URL)
psql "postgresql://user:password@host:port/database"

# Затем выполнить:
\d review
```

## 📋 Быстрая проверка без БД

Если не можете подключиться к БД, проверьте через API:

1. **Запустите сервер**: `npm run dev`
2. **Проверьте endpoint**: `curl http://localhost:9000/store/products/ID/reviews`
3. **Если работает** - значит всё настроено правильно!

## 🎯 Главное

**Миграции уже выполнены успешно!** Таблица создана. Теперь нужно:
1. Запустить сервер
2. Протестировать API
3. Проверить работу через браузер/frontend
