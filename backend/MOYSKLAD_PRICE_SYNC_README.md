# Синхронизация розничных цен из МойСклад

## ✅ Что реализовано

1. **MoySklad Service** - методы для получения цен:
   - `retrievePriceTypes()` - получить все типы цен
   - `retrieveAllRetailPrices()` - получить розничные цены для всех товаров

2. **Price Sync Job** - джоб для периодической синхронизации:
   - Запускается каждый час
   - Получает розничные цены из МойСклад
   - Логирует полученные данные

## 📋 Следующие шаги

Для полной интеграции нужно понять, как в вашем проекте работают цены в Medusa.

### Вариант 1: Использовать скрипт импорта

Создать скрипт, который обновит цены один раз:

```bash
cd backend
npx medusa exec ./src/scripts/import-moysklad-prices.ts
```

###  Вариант 2: Интеграция через API

Нужно узнать:
1. Какая версия Medusa используется?
2. Как сейчас устроены цены товаров?
3. Есть ли у товаров уже price sets?

## 🔍 Проверка данных

Чтобы посмотреть, какие цены получаются из МойСклад:

```bash
# Показать все типы цен
MOYSKLAD_TOKEN=ваш_токен node list-price-types.js
```

## 💡 Структура цен в МойСклад

Цены хранятся в поле `salePrices` каждого товара:

```json
{
  "code": "SKU123",
  "salePrices": [
    {
      "value": 50000,  // 500.00 сум (в тыинах)
      "priceType": {
        "name": "Розничная цена"
      }
    }
  ]
}
```

Сервис автоматически:
- Фильтрует по типу цены "Розничная цена"
- Конвертирует из тыинов в сумы (делит на 100)
- Возвращает Map<SKU, price in UZS>

## ⚙️ Конфигурация

Переменные окружения:

```bash
# Обязательная
MOYSKLAD_TOKEN=ваш_токен

# Опциональная - если тип цены называется по-другому
MOYSKLAD_RETAIL_PRICE_TYPE="Название вашего типа цены"
```
