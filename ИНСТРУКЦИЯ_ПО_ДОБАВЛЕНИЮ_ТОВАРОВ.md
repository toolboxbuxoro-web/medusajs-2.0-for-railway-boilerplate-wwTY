# 📦 Инструкция по добавлению товаров и настройке магазина в Medusa 2.0

## 📋 Содержание
1. [Обзор](#обзор)
2. [Способы добавления товаров](#способы-добавления-товаров)
3. [Через Admin панель (рекомендуется)](#через-admin-панель-рекомендуется)
4. [Через API](#через-api)
5. [Через скрипты seed](#через-скрипты-seed)
6. [Настройка магазина](#настройка-магазина)
7. [Примеры товаров](#примеры-товаров)

---

## 🎯 Обзор

Medusa 2.0 — это гибкая платформа для электронной коммерции с открытым исходным кодом. Для управления товарами доступно несколько способов:

- **Admin панель** (самый простой способ)
- **REST API** (для автоматизации)
- **Seed скрипты** (для массового добавления)

---

## 🚀 Способы добавления товаров

### 1. Через Admin панель (рекомендуется)

#### Шаг 1: Запуск Admin панели

Admin панель уже встроена в backend и запускается автоматически при запуске сервера.

```bash
cd backend
npm run dev
# или
pnpm dev
```

После запуска Admin панель будет доступна по адресу:
- **http://localhost:9000/app**

#### Шаг 2: Вход в Admin панель

1. Откройте браузер и перейдите на `http://localhost:9000/app`
2. При первом запуске создайте администратора:
   - Email: `admin@medusa-test.com`
   - Password: `supersecret`

#### Шаг 3: Добавление товара

1. В левом меню выберите **"Products"** (Товары)
2. Нажмите кнопку **"+ New Product"** (Новый товар)
3. Заполните форму:

   **Основная информация:**
   - **Title** - Название товара (обязательно)
   - **Subtitle** - Подзаголовок (опционально)
   - **Description** - Описание товара
   - **Handle** - URL-адрес товара (например: `cordless-drill-18v`)
   - **Status** - Статус: `Published` (опубликован) или `Draft` (черновик)
   - **Weight** - Вес в граммах

   **Категории:**
   - Выберите категорию из списка или создайте новую

   **Изображения:**
   - Нажмите **"Add Image"**
   - Загрузите изображения товара
   - Первое изображение будет использоваться как миниатюра

   **Опции товара (Options):**
   - Нажмите **"+ Add Option"**
   - Например: `Size` со значениями `S, M, L, XL`
   - Или: `Color` со значениями `Black, White, Red`

   **Варианты (Variants):**
   - После создания опций автоматически создадутся варианты
   - Для каждого варианта укажите:
     - **SKU** - Артикул товара (уникальный)
     - **Price** - Цена в разных валютах
     - **Inventory** - Количество на складе

   **Sales Channels:**
   - Выберите канал продаж (обычно "Default Sales Channel")

4. Нажмите **"Save"** для сохранения товара

---

### 2. Через API

#### Использование REST API

Medusa предоставляет REST API для программного добавления товаров.

**Пример создания товара через API:**

```bash
curl -X POST http://localhost:9000/admin/products \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -d '{
    "title": "Cordless Drill 18V",
    "description": "Powerful 18V cordless drill driver",
    "handle": "cordless-drill-18v",
    "weight": 1500,
    "status": "published",
    "images": [
      {
        "url": "https://example.com/drill-image.jpg"
      }
    ],
    "options": [
      {
        "title": "Kit",
        "values": ["Basic", "Pro"]
      }
    ],
    "variants": [
      {
        "title": "Basic Kit",
        "sku": "DRILL-18V-BASIC",
        "options": {
          "Kit": "Basic"
        },
        "prices": [
          {
            "amount": 5000,
            "currency_code": "usd"
          }
        ]
      }
    ]
  }'
```

**Получение Admin Token:**

1. Войдите в Admin панель
2. Перейдите в Settings → API Keys
3. Создайте новый Secret API Key
4. Используйте этот токен в заголовке `Authorization`

---

### 3. Через скрипты seed

Для массового добавления товаров можно использовать seed скрипты.

#### Структура товара в seed скрипте

Посмотрите пример в файле `backend/src/scripts/seed-tools.ts`:

```typescript
{
  title: "Cordless Drill 18V",
  subtitle: "Дрель-шуруповерт аккумуляторная",
  description: "Powerful 18V cordless drill driver...",
  handle: "cordless-drill-18v",
  weight: 1500,
  status: ProductStatus.PUBLISHED,
  shipping_profile_id: shippingProfile.id,
  category_ids: [categoryId],
  images: [
    { url: "https://example.com/image.jpg" }
  ],
  options: [
    { title: "Kit", values: ["Basic", "Pro"] }
  ],
  variants: [
    {
      title: "Basic Kit",
      sku: "DRILL-18V-BASIC",
      options: { Kit: "Basic" },
      prices: [
        { amount: 5000, currency_code: "usd" },
        { amount: 4500, currency_code: "eur" }
      ]
    }
  ],
  sales_channels: [{ id: defaultSalesChannel.id }]
}
```

#### Запуск seed скрипта

```bash
cd backend
npm run ib  # или pnpm ib
```

Это выполнит миграции и заполнит базу данных тестовыми данными.

---

## ⚙️ Настройка магазина

### 1. Настройка регионов (Regions)

Регионы определяют, в каких странах доступны товары и какие валюты используются.

**Через Admin панель:**
1. Settings → Regions
2. Создайте новый регион или отредактируйте существующий
3. Укажите:
   - Название региона
   - Валюту по умолчанию
   - Страны
   - Налоговые настройки

**Пример через seed скрипт:**

```typescript
await createRegionsWorkflow(container).run({
  input: {
    regions: [
      {
        name: "Europe",
        currency_code: "eur",
        countries: ["gb", "de", "dk", "se", "fr", "es", "it"],
        payment_providers: ["pp_system_default"],
      },
    ],
  },
});
```

### 2. Настройка Sales Channels

Sales Channels определяют, где продаются товары (веб-сайт, мобильное приложение и т.д.).

**Через Admin панель:**
1. Settings → Sales Channels
2. Создайте новый канал или используйте "Default Sales Channel"
3. Привяжите канал к API ключам

### 3. Настройка Shipping (Доставка)

**Через Admin панель:**
1. Settings → Shipping
2. Создайте Shipping Profile
3. Добавьте Shipping Options:
   - Standard Shipping
   - Express Shipping
4. Настройте цены для разных регионов

**Пример через seed скрипт:**

```typescript
await createShippingOptionsWorkflow(container).run({
  input: [
    {
      name: "Standard Shipping",
      price_type: "flat",
      provider_id: "manual_manual",
      service_zone_id: fulfillmentSet.service_zones[0].id,
      shipping_profile_id: shippingProfile.id,
      prices: [
        {
          currency_code: "usd",
          amount: 1000, // $10.00
        },
        {
          currency_code: "eur",
          amount: 1000, // €10.00
        },
      ],
    },
  ],
});
```

### 4. Настройка Payment Providers

**Через Admin панель:**
1. Settings → Payment Providers
2. Выберите провайдера (Stripe, PayPal и т.д.)
3. Введите API ключи
4. Активируйте провайдера

### 5. Настройка Stock Locations (Склады)

**Через Admin панель:**
1. Settings → Stock Locations
2. Создайте склад
3. Укажите адрес
4. Привяжите к Sales Channels

---

## 📝 Примеры товаров

### Пример 1: Простой товар без вариантов

```typescript
{
  title: "Hammer",
  description: "Professional construction hammer",
  handle: "hammer",
  weight: 500,
  status: ProductStatus.PUBLISHED,
  shipping_profile_id: shippingProfile.id,
  images: [
    { url: "https://example.com/hammer.jpg" }
  ],
  variants: [
    {
      title: "Default",
      sku: "HAMMER-001",
      prices: [
        { amount: 2000, currency_code: "usd" }
      ]
    }
  ],
  sales_channels: [{ id: defaultSalesChannel.id }]
}
```

### Пример 2: Товар с вариантами (размер и цвет)

```typescript
{
  title: "T-Shirt",
  description: "Cotton T-shirt",
  handle: "t-shirt",
  weight: 200,
  status: ProductStatus.PUBLISHED,
  shipping_profile_id: shippingProfile.id,
  images: [
    { url: "https://example.com/tshirt-black.jpg" },
    { url: "https://example.com/tshirt-white.jpg" }
  ],
  options: [
    {
      title: "Size",
      values: ["S", "M", "L", "XL"]
    },
    {
      title: "Color",
      values: ["Black", "White"]
    }
  ],
  variants: [
    {
      title: "S / Black",
      sku: "TSHIRT-S-BLACK",
      options: {
        Size: "S",
        Color: "Black"
      },
      prices: [
        { amount: 1500, currency_code: "usd" }
      ]
    },
    {
      title: "M / White",
      sku: "TSHIRT-M-WHITE",
      options: {
        Size: "M",
        Color: "White"
      },
      prices: [
        { amount: 1500, currency_code: "usd" }
      ]
    }
    // ... другие варианты
  ],
  sales_channels: [{ id: defaultSalesChannel.id }]
}
```

### Пример 3: Товар с категорией

```typescript
// Сначала создайте категорию
const { result: categoryResult } = await createProductCategoriesWorkflow(
  container
).run({
  input: {
    product_categories: [
      {
        name: "Power Tools",
        handle: "power-tools",
        is_active: true,
      },
    ],
  },
});

// Затем создайте товар с категорией
{
  title: "Cordless Drill",
  category_ids: [categoryResult[0].id],
  // ... остальные поля
}
```

---

## 🔧 Важные настройки перед добавлением товаров

### 1. Проверьте наличие:

- ✅ **Sales Channel** - должен быть создан "Default Sales Channel"
- ✅ **Shipping Profile** - должен быть создан профиль доставки
- ✅ **Stock Location** - должен быть создан склад
- ✅ **Region** - должен быть создан хотя бы один регион
- ✅ **Currency** - должна быть настроена валюта

### 2. Настройка изображений

Для хранения изображений можно использовать:

- **MinIO** (встроен в Railway template)
- **AWS S3**
- **DigitalOcean Spaces**
- **Локальное хранилище** (для разработки)

**Настройка MinIO:**
Переменные окружения уже настроены в Railway template. Изображения будут автоматически сохраняться в MinIO.

---

## 📚 Полезные ссылки

- [Официальная документация Medusa](https://docs.medusajs.com/)
- [Medusa Admin API Reference](https://docs.medusajs.com/api/admin)
- [Medusa Store API Reference](https://docs.medusajs.com/api/store)
- [Medusa GitHub](https://github.com/medusajs/medusa)

---

## 🎯 Быстрый старт

1. **Запустите backend:**
   ```bash
   cd backend
   npm run dev
   ```

2. **Откройте Admin панель:**
   - URL: http://localhost:9000/app
   - Создайте администратора при первом входе

3. **Добавьте первый товар:**
   - Products → + New Product
   - Заполните форму
   - Сохраните

4. **Проверьте на storefront:**
   - URL: http://localhost:3000
   - Товар должен появиться на сайте

---

## ⚠️ Важные замечания

1. **Handle товара** должен быть уникальным и URL-friendly
2. **SKU варианта** должен быть уникальным
3. **Цены** указываются в минимальных единицах (например, 1000 = $10.00)
4. **Статус товара** должен быть `published` для отображения на сайте
5. **Sales Channel** должен быть привязан к товару
6. **Изображения** должны быть доступны по URL или загружены в хранилище

---

## 🆘 Решение проблем

**Товар не отображается на сайте:**
- Проверьте статус товара (должен быть `published`)
- Проверьте привязку к Sales Channel
- Проверьте наличие вариантов с ценами
- Проверьте наличие товара на складе

**Ошибка при создании товара:**
- Проверьте все обязательные поля
- Убедитесь, что Shipping Profile создан
- Убедитесь, что Sales Channel создан
- Проверьте формат данных (особенно цены)

---

**Удачной работы с Medusa! 🚀**

